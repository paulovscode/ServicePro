# ====================
# SERVER & APPLICATION
# ====================
server.port=8082
server.servlet.context-path=/
spring.application.name=servicepro-backend

# ====================
# DATABASE (MySQL)
# ====================
spring.datasource.url=jdbc:mysql://localhost:3306/servicepro_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ====================
# JPA / HIBERNATE
# ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# ====================
# JWT SECURITY
# ====================
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# ====================
# EMAIL CONFIGURATION
# ====================
# Configuração para Gmail (exemplo)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=paulovitorms04@gmail.com
spring.mail.password=jlni oxww heol mdxh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ====================
# THYMELEAF (para templates de email)
# ====================
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML5
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# ====================
# APPLICATION PROPERTIES
# ====================
app.frontend.url=http://localhost:3000
app.email.from=nao-responder@servicepro.com.br
app.url=http://localhost:8082

# ====================
# FILE UPLOAD (para fotos de perfil)
# ====================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ====================
# LOGGING
# ====================
logging.level.com.servicepro=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.security=DEBUG

# ====================
# CORS (para desenvolvimento)
# ====================
cors.allowed-origins=http://localhost:3000,http://localhost:8081
cors.allowed-methods=GET,POST,PUT,PATCH,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
spring.main.allow-bean-definition-overriding=true

